""": componente (cadena), marca (cadena), cantidad (entero), precio (decimal)
"""

class ComponenteCPU:
    def __init__(self,componente, marca, cantidad, precio):

        self.componente = componente
        self.marca = marca
        self.cantidad = cantidad
        self.precio = precio
